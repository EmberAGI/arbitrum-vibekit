# Pendle agent env (runtime).
#
# Copy this file to `.env` to run locally (file is gitignored).

# Optional: set to `production` for production behavior, otherwise defaults to debug behavior.
PENDLE_MODE=debug

# Optional: bypass delegation signing by using the agent wallet for execution.
DELEGATIONS_BYPASS=false

# Optional: LangGraph API (defaults shown).
LANGGRAPH_DEPLOYMENT_URL=http://localhost:8125
LANGGRAPH_GRAPH_ID=agent-pendle

# Optional: configure cron/thread behavior for the API-driven runner.
# STARTER_THREAD_ID=
# STARTER_CRON_INTERVAL_MS=5000
# STARTER_CRON_EXPRESSION=*/5 * * * * *

# Optional: poll loop tuning (defaults: 3600000ms = 1h, -1 stream limit, 100 history).
PENDLE_POLL_INTERVAL_MS=3600000
PENDLE_STREAM_LIMIT=-1
PENDLE_STATE_HISTORY_LIMIT=100

# Onchain actions API (optional override; defaults to https://api.emberai.xyz)
# ONCHAIN_ACTIONS_API_URL=http://localhost:50051

# Optional: signing key used when tx execution mode resolves to `execute`.
# Use a throwaway dev key with small funds; never commit real keys.
A2A_TEST_AGENT_NODE_PRIVATE_KEY=

# Optional: chain override for scripts (defaults to Arbitrum One when unset).
CLMM_CHAIN_ID=42161

# Optional: point at a directory containing `service.json` (otherwise auto-discovers `config/service.json`).
# AGENT_CONFIG_DIR=./config
