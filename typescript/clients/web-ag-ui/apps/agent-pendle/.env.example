# Optional: bypass delegation signing by using the agent wallet for execution
DELEGATIONS_BYPASS=false

# Onchain actions API (optional override; defaults to https://api.emberai.xyz)
ONCHAIN_ACTIONS_API_URL=https://api.emberai.xyz

# Optional: poll interval for cron loop (default: 3600000 = 1h)
# PENDLE_POLL_INTERVAL_MS=3600000
