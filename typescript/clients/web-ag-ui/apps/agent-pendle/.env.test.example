# CI-safe defaults for integration/e2e tests.
# Copy this file to `.env.test` to override locally (file is gitignored).

# Optional: bypass delegation signing by using the agent wallet for execution.
DELEGATIONS_BYPASS=false

# Optional: LangGraph API (defaults shown).
LANGGRAPH_DEPLOYMENT_URL=http://localhost:8125
LANGGRAPH_GRAPH_ID=agent-pendle

# Optional: onchain actions API URL used by the agent runtime.
# Defaults to https://api.emberai.xyz when unset; CI typically points at a local stub.
ONCHAIN_ACTIONS_API_URL=http://localhost:50051

# Optional: signing key used when tx execution mode resolves to `execute` (eg DELEGATIONS_BYPASS=true).
# Use a throwaway dev key with small funds; never commit real keys.
A2A_TEST_AGENT_NODE_PRIVATE_KEY=

# Smoke: delegated execution (manual, not CI).
# Delegator key signs a single delegation for the executor to redeem. Keep this wallet funded on Arbitrum.
PENDLE_SMOKE_DELEGATOR_PRIVATE_KEY=

# Optional: market/funding token/amount overrides used for planning.
# PENDLE_SMOKE_MARKET_ADDRESS=0x0000000000000000000000000000000000000000
# PENDLE_SMOKE_FUNDING_TOKEN_ADDRESS=0x0000000000000000000000000000000000000000
# PENDLE_SMOKE_AMOUNT_BASE_UNITS=1000000

# Optional: set true to submit live txs (default is simulate-only).
# PENDLE_SMOKE_EXECUTE=false

# Optional: enable positions lookup during smoke runs.
# SMOKE_WALLET=0x0000000000000000000000000000000000000000
