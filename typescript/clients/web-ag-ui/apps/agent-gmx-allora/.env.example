# Optional: bypass delegation signing by using an agent-controlled wallet for submission
DELEGATIONS_BYPASS=false

# Optional: enable embedded-wallet transaction submission for local/dev
# Supported: plan | submit
GMX_ALLORA_TX_SUBMISSION_MODE=plan
# Required when GMX_ALLORA_TX_SUBMISSION_MODE=submit (0x + 64 hex chars)
GMX_ALLORA_EMBEDDED_PRIVATE_KEY=
