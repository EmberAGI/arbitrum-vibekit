# CI-safe defaults for integration/e2e tests.
# Copy this file to `.env.test` to override locally (file is gitignored).
CLMM_E2E_PRIVATE_KEY=replace-with-private-key
# Optional: ensure the live wallet matches the private key address.
# CLMM_E2E_LIVE_WALLET=0x0000000000000000000000000000000000000000
# Optional: pin the Camelot pool address used in e2e.
# CLMM_E2E_POOL_ADDRESS=0x0000000000000000000000000000000000000000
# Optional: limit how many pools are scanned when auto-selecting a funded pool.
# CLMM_E2E_POOL_SCAN_LIMIT=20
# Optional: override the USD allocation used for accounting (must be <= wallet pool token USD).
# CLMM_E2E_ALLOCATION_USD=10
# Optional: override Ember API base URL.
# EMBER_API_BASE_URL=https://api.emberai.xyz
# Optional: increase live test timeout (milliseconds).
# EMBER_E2E_TIMEOUT_MS=180000
# Optional: provide a real Arbitrum RPC endpoint for live txs.
# ARBITRUM_RPC_URL=https://arb-mainnet.g.alchemy.com/v2/replace-with-key
