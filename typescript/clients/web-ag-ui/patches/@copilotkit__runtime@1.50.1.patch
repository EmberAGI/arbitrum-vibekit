diff --git a/dist/index.js b/dist/index.js
index fd5b8a29eabb222e8c11e48104bfc20f392bb1b6..44f99f14612400d371c75c6c25a7cb717074a27d 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -3300,6 +3300,11 @@ var TelemetryAgentRunner = class {
   * Delegates to the underlying runner's connect method
   */
   connect(...args) {
+    const input = args[0];
+    console.debug("[TelemetryAgentRunner.connect]", {
+      runner: this._runner && this._runner.constructor ? this._runner.constructor.name : typeof this._runner,
+      threadId: input == null ? void 0 : input.threadId
+    });
     return this._runner.connect(...args);
   }
   /**
@@ -3581,8 +3586,9 @@ var CopilotRuntime = class {
       var _a, _b, _c, _d;
       const { request } = hookParams;
       const publicApiKey = request.headers.get("x-copilotcloud-public-api-key");
-      const body = await shared.readBody(request);
-      const forwardedProps = body.forwardedProps;
+      const body = await shared.readBody(request) || {};
+      const forwardedProps = typeof body.forwardedProps === "object" && body.forwardedProps !== null ? body.forwardedProps : void 0;
+      const messages = Array.isArray(body.messages) ? body.messages : [];
       const cloudBaseUrl = process.env.COPILOT_CLOUD_BASE_URL || "https://api.cloud.copilotkit.ai";
       telemetry_client_default.capture("oss.runtime.copilot_request_created", {
         "cloud.guardrails.enabled": ((_a = forwardedProps == null ? void 0 : forwardedProps.cloud) == null ? void 0 : _a.guardrails) !== void 0,
@@ -3596,7 +3602,7 @@ var CopilotRuntime = class {
       (_c = params == null ? void 0 : params.beforeRequestMiddleware) == null ? void 0 : _c.call(params, hookParams);
       if ((_d = params == null ? void 0 : params.middleware) == null ? void 0 : _d.onBeforeRequest) {
         const { request: request2, runtime, path } = hookParams;
-        const gqlMessages = aguiToGQL(body.messages).reduce((acc, msg) => {
+        const gqlMessages = aguiToGQL(messages).reduce((acc, msg) => {
           if ("role" in msg && msg.role === "user") {
             acc.inputMessages.push(msg);
           } else {
diff --git a/dist/index.mjs b/dist/index.mjs
index cdf20397ff2299da69ff0a270fced67aa074d3fc..15850538d479d07e8c5315aa6b6efedb4561d8c3 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -3293,6 +3293,11 @@ var TelemetryAgentRunner = class {
   * Delegates to the underlying runner's connect method
   */
   connect(...args) {
+    const input = args[0];
+    console.debug("[TelemetryAgentRunner.connect]", {
+      runner: this._runner && this._runner.constructor ? this._runner.constructor.name : typeof this._runner,
+      threadId: input == null ? void 0 : input.threadId
+    });
     return this._runner.connect(...args);
   }
   /**
@@ -3574,8 +3579,9 @@ class CopilotRuntime {
       var _a, _b, _c, _d;
       const { request } = hookParams;
       const publicApiKey = request.headers.get("x-copilotcloud-public-api-key");
-      const body = await readBody(request);
-      const forwardedProps = body.forwardedProps;
+      const body = await readBody(request) || {};
+      const forwardedProps = typeof body.forwardedProps === "object" && body.forwardedProps !== null ? body.forwardedProps : void 0;
+      const messages = Array.isArray(body.messages) ? body.messages : [];
       const cloudBaseUrl = process.env.COPILOT_CLOUD_BASE_URL || "https://api.cloud.copilotkit.ai";
       telemetry_client_default.capture("oss.runtime.copilot_request_created", {
         "cloud.guardrails.enabled": ((_a = forwardedProps == null ? void 0 : forwardedProps.cloud) == null ? void 0 : _a.guardrails) !== void 0,
@@ -3589,7 +3595,7 @@ class CopilotRuntime {
       (_c = params == null ? void 0 : params.beforeRequestMiddleware) == null ? void 0 : _c.call(params, hookParams);
       if ((_d = params == null ? void 0 : params.middleware) == null ? void 0 : _d.onBeforeRequest) {
         const { request: request2, runtime, path } = hookParams;
-        const gqlMessages = aguiToGQL(body.messages).reduce((acc, msg) => {
+        const gqlMessages = aguiToGQL(messages).reduce((acc, msg) => {
           if ("role" in msg && msg.role === "user") {
             acc.inputMessages.push(msg);
           } else {
