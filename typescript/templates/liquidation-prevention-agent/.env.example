# Liquidation Prevention Agent Environment Configuration
# Copy this file to .env and fill in your actual values

# ========================================
# REQUIRED: Blockchain & Transaction Setup
# ========================================

# Your wallet private key (required for transaction execution)
USER_PRIVATE_KEY=your_private_key_here

# QuickNode RPC endpoint configuration (required for transaction execution)
QUICKNODE_SUBDOMAIN=your_quicknode_subdomain
QUICKNODE_API_KEY=your_quicknode_api_key

# ========================================
# REQUIRED: AI & MCP Configuration  
# ========================================

# OpenRouter API key for LLM (required for intelligent prevention strategies)
OPENROUTER_API_KEY=your_openrouter_api_key

# ========================================
# OPTIONAL: AI Configuration
# ========================================

# LLM model to use (default: deepseek/deepseek-chat-v3-0324:free)
LLM_MODEL=deepseek/deepseek-chat-v3-0324:free

# AI provider preference (default: openrouter)
AI_PROVIDER=openrouter

# AI model override (alternative to LLM_MODEL)
AI_MODEL=

# Ember MCP server endpoint (default: https://api.emberai.xyz/mcp)
EMBER_ENDPOINT=https://api.emberai.xyz/mcp

# ========================================
# OPTIONAL: Health Factor Thresholds
# ========================================

# Warning threshold - alerts start here (default: 1.5)
HEALTH_FACTOR_WARNING=1.5

# Danger threshold - urgent action needed (default: 1.1)  
HEALTH_FACTOR_DANGER=1.1

# Critical threshold - immediate action required (default: 1.03)
HEALTH_FACTOR_CRITICAL=1.03

# ========================================
# OPTIONAL: Monitoring Configuration
# ========================================

# Monitoring interval in milliseconds (default: 900000 = 15 minutes)
MONITORING_INTERVAL=900000

# Maximum retry attempts for failed operations (default: 3)
MAX_RETRY_ATTEMPTS=3

# Gas price multiplier for transaction safety (default: 1.5)
GAS_PRICE_MULTIPLIER=1.5

# ========================================
# OPTIONAL: Agent Configuration
# ========================================

# Agent basic settings
AGENT_NAME=Liquidation Prevention Agent
AGENT_VERSION=1.0.0
AGENT_DESCRIPTION=Intelligent Aave liquidation prevention agent with monitoring and automatic risk mitigation
AGENT_URL=localhost

# Server configuration
PORT=3010
ENABLE_CORS=true
BASE_PATH=

# ========================================
# OPTIONAL: Development Settings
# ========================================

# Environment settings
NODE_ENV=development
DEBUG_MODE=false

# Token caching (default: false)
AGENT_CACHE_TOKENS=false

# MCP tool timeout in milliseconds (default: 30000)
MCP_TOOL_TIMEOUT_MS=30000

# ========================================
# CONFIGURATION USAGE NOTES
# ========================================

# User Preference Parsing:
# The agent can parse preferences from natural language instructions:
# - "Monitor with health factor 1.2 and check every 1 minute"
# - "Use conservative approach with 1.3 threshold"  
# - "Check every 5 minutes with aggressive prevention"

# Default Values (automatically applied if not specified):
# - Health Factor Thresholds: Warning 1.5, Danger 1.1, Critical 1.03
# - Monitoring Interval: 1 minute (60000ms)
# - Transaction retries: 3 attempts with 1.5x gas multiplier

# Environment variables override user preferences and defaults
# Priority order: Environment Variables > User Preferences > Defaults