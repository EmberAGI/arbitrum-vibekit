# ===========================
# ALLORA TRADING AGENT CONFIGURATION
# ===========================

# REQUIRED: Allora API Key for accessing trading predictions
ALLORA_API_KEY=

# This agent uses common environment variables from the main typescript/.env file:
# - OPENROUTER_API_KEY (AI provider)
# - EMBER_ENDPOINT (Ember API endpoint)

# ===========================
# AGENT-SPECIFIC CONFIGURATION (Optional)
# ===========================

# Agent metadata (defaults shown)
AGENT_NAME=Allora Trading Agent
AGENT_VERSION=1.0.0
AGENT_DESCRIPTION=AI agent for trading predictions using Allora

# Port configurations
PORT=3008                    # Main agent port
ALLORA_MCP_PORT=3009        # Allora MCP server port
EMBER_MCP_PORT=3010         # Ember MCP server port

# LLM model selection (defaults to google/gemini-2.5-flash-preview)
# LLM_MODEL=google/gemini-2.5-flash-preview

# Logging configuration
# LOG_LEVEL=info