# ===========================
# LENDING AGENT CONFIGURATION
# ===========================

# This agent uses common environment variables from the main typescript/.env file:
# - OPENROUTER_API_KEY (AI provider)
# - EMBER_ENDPOINT (Ember API endpoint)
# - QUICKNODE_SUBDOMAIN & QUICKNODE_API_KEY (Blockchain RPC)
# - AGENT_CACHE_TOKENS (Performance setting)
# - MCP_TOOL_TIMEOUT_MS (Timeout configuration)

# ===========================
# AGENT-SPECIFIC OVERRIDES (Optional)
# ===========================

# Override the default MCP timeout if this agent needs longer timeouts
# MCP_TOOL_TIMEOUT_MS=90000

# Override the default token caching behavior for this agent
# AGENT_CACHE_TOKENS=true

# Agent metadata (optional overrides)
AGENT_NAME=Lending Agent
AGENT_VERSION=1.0.0
AGENT_DESCRIPTION=AAVE lending operations agent

# Port configuration (defaults to 3001)
# PORT=3001